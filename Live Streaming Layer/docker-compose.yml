version: "3.8"

services:
  live_streaming_layer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: live_streaming_layer
    networks:
      - messaging_broker_layer_default
      
networks:
  messaging_broker_layer_default:
    external: true


